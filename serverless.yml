service: restaurant-service

provider:
  name: aws
  runtime: nodejs16.x
  timeout: 8
  versionFunctions: false
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - dynamodb:DescribeTable
            - dynamodb:Query
            - dynamodb:Scan
            - dynamodb:GetItem
            - dynamodb:PutItem
            - dynamodb:UpdateItem
            - dynamodb:DeleteItem
          Resource: "arn:aws:dynamodb:us-east-1:*:*"

functions:
  restaurants:
    handler: handler.restaurants
    events:
      - http:
          path: /restaurants
          method: ANY

  food:
    handler: restaurants.food
    events:
      - http:
          path: /restaurants/{restaurantId}/food
          method: ANY

  orders:
    handler: restaurants.orders
    events:
      - http:
          path: /restaurants/{restaurantId}/orders
          method: ANY

resources:
  Resources:
    RestaurantsTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: Restaurants
        AttributeDefinitions:
          - AttributeName: id
            AttributeType: S
        KeySchema:
          - AttributeName: id
            KeyType: HASH
        ProvisionedThroughput:
          ReadCapacityUnits: 1
          WriteCapacityUnits: 1

package:
  patterns:
    - "!node_modules/**"
